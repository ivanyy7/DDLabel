{
  "name": "ddlabel",
  "private": true,
  "description": "DDLabel — печать этикеток по голосу",
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev",
    "server": "cd server && npm start",
    "migrate:shelf": "node scripts/migrate-shelf-to-vercel.js"
  },
  "engines": {
    "node": ">=18"
  }
}
